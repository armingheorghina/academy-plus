libft-test
==========

To install, copy unit_test/ at the root of your lib, then : "make && ./unit_test"
OR
Copy unit_test/ elsewhere and put libft.a in the same directory with unit_test/, then : "make && ./unit_test"
